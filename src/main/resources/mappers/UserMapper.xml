<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserReposity">
    <select id="findAll" resultType="com.example.model.User">
        select * from user
    </select>
    <!-- 根据 userID 查询用户 -->
    <select id="findByUserId" parameterType="int" resultType="com.example.model.User">
        SELECT * FROM [user]
        WHERE userID = #{userId};
    </select>

    <!-- 根据 username 查询用户 -->
    <select id="findByUsername" parameterType="String" resultType="com.example.model.User">
        SELECT * FROM [user]
        WHERE username = #{username};
    </select>

</mapper>